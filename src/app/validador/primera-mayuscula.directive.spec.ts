import { PrimeraMayusculaDirective } from './primera-mayuscula.directive';

describe('PrimeraMayusculaDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimeraMayusculaDirective();
    expect(directive).toBeTruthy();
  });
});
