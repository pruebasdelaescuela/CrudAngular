import { ValidanombreDirective } from './validanombre.directive';

describe('ValidanombreDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidanombreDirective();
    expect(directive).toBeTruthy();
  });
});
